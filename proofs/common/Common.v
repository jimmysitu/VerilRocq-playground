Require Export Pfv.Lib.Common.
Require Export Pfv.Lib.Lib.

Local Open Scope hmap_scope.

(*
Definition hsingle_opt (i: vid_t) (v: hmap) : hmap :=
  if is_empty v then HMapEmpty else HMapStr [(i, v)].
*)
